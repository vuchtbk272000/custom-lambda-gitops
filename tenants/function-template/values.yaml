nameOverride: ""
fullnameOverride: ""

namespace:
  name: tenant-lambda

# Default secret names used by every Function unless overridden per-function.
# Create these Secrets in the tenant namespace (bootstrap script / Sealed Secrets).
imageCredentials:
  name: push-secret

gitCredentials:
  name: git-repo-secret

build:
  builder: openfunction/gcp-builder:v1

serving:
  runtime: knative
  imagePullPolicy: IfNotPresent

# Optional: render Secret CRs from values (default off — use bootstrap instead).
secrets:
  enabled: true
  registry:
    server: "https://index.docker.io/v1/"
    username: "vuxucam"
    password: "dckr_pat_9VHZwt8bmyDNenYuxTbQM34tNpo"
    email: "vuchtbk272000@gmail.com"
    auth: ""
  git:
    username: "vuchtbk272000"
    password: "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"

# Per-function values. Omit imageCredentials / git credentials / builder / serving
# to inherit the defaults above. Override only when a function needs something different.
functions: []
# - name: function-1
#   version: "v1.0.0"
#   image: "registry/image:v1"
#   build:
#     env:
#       GOOGLE_FUNCTION_TARGET: "hello_world"
#       GOOGLE_FUNCTION_SIGNATURE_TYPE: "http"
#       GOOGLE_FUNCTION_SOURCE: "main.py"
#     srcRepo:
#       url: "https://github.com/org/repo.git"
#       sourceSubPath: "function-1"
#       revision: "COMMIT_SHA"
#   # Optional overrides:
#   # imageCredentials:
#   #   name: custom-push-secret
#   # build:
#   #   builder: other-builder:tag
#   #   srcRepo:
#   #     credentials:
#   #       name: custom-git-secret
#   # serving:
#   #   imagePullPolicy: Always
